import streamlit as st import yfinance as yf import requests import time from datetime import datetime, timedelta # --- 🟢 PAGE CONFIG (Makes it look like an app) --- st.set_page_config(page_title="AI Trading Advisor", page_icon="📈", layout="wide") st.title("🌟 AI Trading Advisor V2.0") st.write(f"**Live Market Intelligence** | {datetime.now().strftime('%B %d, %Y')}") # --- 🟢 SETUP --- API_KEY = "d6jgtchr01qkvh5prf7gd6jgtchr01qkvh5prf80" WATCHLIST = [ "AAPL", "MSFT", "NVDA", "GOOGL", "AMZN", "META", "TSLA", "AVGO", "LLY", "JPM", "UNH", "XOM", "V", "MA", "PG", "COST", "HD", "JNJ", "ABBV", "BAC", "WMT", "KO", "CRM", "NFLX", "ADBE", "AMD", "ORCL", "PEP", "CVX", "MRK", "TMO", "WFC", "CSCO", "ACN", "MCD", "ABT", "LIN", "DHR", "INTC", "INTU", "DIS", "TXN", "PM", "VZ", "AMAT", "CAT", "PFE", "UNP", "IBM", "AMGN", "NOW", "LOW", "ISRG", "QCOM", "HON", "GE", "AXP", "SPGI", "T", "BA", "GS", "NEE", "BKNG", "PLD", "MS", "RTX", "TJX", "VRTX", "ELV", "ADP", "MDT", "SYK", "BLK", "ETN", "MMC", "LMT", "SCHW", "ADI", "MDLZ", "CB", "AMT", "CI", "C", "GILD", "BMY", "MU", "LRCX", "DE", "REGN", "ZTS", "PLTR", "UBER", "SNPS", "PANW", "CDNS", "KLAC", "BSX", "VRSK", "MNST", "ORLY" ] def get_market_signal(ticker): try: # 1. Get News from Finnhub to_date = datetime.now().strftime('%Y-%m-%d') from_date = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d') news_url = f"https://finnhub.io/api/v1/company-news?symbol={ticker}&from={from_date}&to={to_date}&token={API_KEY}" news_data = requests.get(news_url).json() if not news_data: return "No news found.", "🟡 HOLD" headline = news_data[0].get('headline', "No Headline") # 2. Get Price Data from yfinance stock = yf.Ticker(ticker) hist = stock.history(period="1d", interval="1m") if hist.empty: return headline, "⚪️ MARKET CLOSED" price = hist['Close'].iloc[-1] change = ((price - hist['Open'].iloc[0]) / hist['Open'].iloc[0]) * 100 # 3. Decision Logic bad_news = ["war", "plunge", "hit", "drop", "fire", "attack", "outage"] good_news = ["growth", "buy", "target", "soar", "deal", "ai"] headline_low = headline.lower() is_bad = any(w in headline_low for w in bad_news) is_good = any(w in headline_low for w in good_news) if is_bad and change < -0.5: advice = f"🔴 RECOMMEND: SELL (News: {change:.2f}%)" elif is_good and change > 0.5: advice = f"🟢 RECOMMEND: BUY (News: {change:.2f}%)" else: advice = f"🟡 RECOMMEND: HOLD" return headline, advice except Exception as e: return f"Error: {e}", "❌ ERROR" # --- 🚀 THE APP INTERFACE --- if st.button('🔍 Start Real-Time Scan'): st.write("---") progress_bar = st.progress(0) status_text = st.empty() # We create a container to show the alerts as they happen alert_container = st.container() for i, ticker in enumerate(WATCHLIST): # Update progress percent_complete = (i + 1) / len(WATCHLIST) progress_bar.progress(percent_complete) status_text.text(f"Analyzing {ticker} ({i+1}/{len(WATCHLIST)})...") headline, advice = get_market_signal(ticker) # Displaying the results in columns with alert_container: col1, col2 = st.columns([1, 4]) with col1: st.write(f"**{ticker}**") with col2: if "SELL" in advice: st.error(advice) elif "BUY" in advice: st.success(advice) else: st.info(advice) st.caption(f"📰 {headline}") st.divider() time.sleep(1.2) # API Protection st.balloons() st.success("Full Scan Complete!") else: st.info("Click the button above to analyze 100 stocks.")
